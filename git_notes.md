# Git

git reset --hard commit <commit>  =>> to certain commit (algun commit que quiera regresar)
git reset --hard origin/master  => regresar a la version original del repositorio.

git branch => en que rama estoy y cuales existen

git checkout => see which branches exist

git checkout -b <name>   => create and move to branch <name>

